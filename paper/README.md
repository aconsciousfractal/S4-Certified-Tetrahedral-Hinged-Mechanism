# Paper source

This directory contains the TeX source, bibliography, generated/support files,
and built PDF for the S4 certified tetrahedral hinged mechanism paper.

Build with the commands in `BUILD.md`.
